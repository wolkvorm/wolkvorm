apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-schemas
  labels:
    {{- include "terraforge.labels" . | nindent 4 }}
data:
  {{- range $path, $_ := .Files.Glob "schemas/*.json" }}
  {{ base $path }}: |-
    {{ $.Files.Get $path | nindent 4 }}
  {{- end }}
